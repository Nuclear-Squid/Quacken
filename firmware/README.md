# Keyberon Firmware

Note: `elf2uf2-rs` does not work “as is” because of [an open
issue](https://github.com/JoNil/elf2uf2-rs/pull/41).

The patched version can be installed with:

```bash
cargo install --git https://github.com/StripedMonkey/elf2uf2-rs.git#c1638b9
```
